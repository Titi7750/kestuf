{% extends 'base.html.twig' %}

{% block title %}Donner un avis sur l'évènement
{% endblock %}

{% block body %}
	<div class="text-center mt-5">
		<a href="{{ path('app_event') }}">
			<img src="{{ asset('images/kestuf_orange.png') }}" alt="Logo Kestuf' en orange">
		</a>
	</div>
	<div class="d-flex justify-content-center align-items-center flex-column gap">
		<h2>Ajouter un commentaire</h2>
		{{form_start(commentForm)}}
		{{form_widget(commentForm)}}
		{{form_end(commentForm)}}
	</div>

{% endblock %}
